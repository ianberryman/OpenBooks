/**
 * @openbooks/plugin-api — the internal module contract (spec §8).
 *
 * Populated by OB-002. Unstable at 0.x by design: the surface is designed
 * against one module in M1 and will be wrong in ways only M2 and M3 reveal.
 */
export const PLUGIN_API_VERSION = '0.0.0' as const;
