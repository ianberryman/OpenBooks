/**
 * Recurring invoices' wire contracts (OB-128; ROADMAP D-75, D-76).
 *
 * Read `recurring.ts` for the template shape and for why `startDate` is create-only
 * and never appears on the response.
 */

export * from './recurring';
