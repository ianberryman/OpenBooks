/**
 * The bank-account setup screen (OB-095). Exported for the Banking section to mount as its
 * "Accounts" tab at integration; this folder does not touch `App.tsx` or the shell nav.
 */
export { BankAccountsScreen } from './bank-accounts';
