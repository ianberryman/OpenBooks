/**
 * The file every `no-float-money` test case pretends to be.
 *
 * RuleTester lints the `code` string it is given, not this content, but the path
 * has to exist and belong to `./tsconfig.json` for the parser to build a program
 * and hand the rule a type checker.
 */
export {};
